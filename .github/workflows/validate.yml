name: Validate Skills

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate skills
        shell: pwsh
        run: .\scripts\validate-skills.ps1
